import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nabstertsr.queuemarshal',
  appName: 'Queue-Marshal',
  webDir: 'dist'
};

export default config;
